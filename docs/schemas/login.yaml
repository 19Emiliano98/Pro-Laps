# login
components:
  requestBodies:
    login:
      type: object
      required:
        - username
        - password
      properties:
        username:
          type: string
          description: Nombre de usuario que se visualiza en la pagina
        password:
          type: string
          description: Pass de la cuenta ( encryptada )
      example:
        username: Emi98
        password: dkcvpeprottrldlfderer